name: flutter_simple_base
description: A new brick created with the Mason CLI.

# The following defines the version and build number for your brick.
# A version number is three numbers separated by dots, like 1.2.34
# followed by an optional build number (separated by a +).
version: 0.1.2+3

# The following defines the environment for the current brick.
# It includes the version of mason that the brick requires.
environment:
  mason: "^0.1.0"

# Variables specify dynamic values that your brick depends on.
# Zero or more variables can be specified for a given brick.
# Each variable has:
#  * a type (string, number, boolean, enum, or array)
#  * an optional short description
#  * an optional default value
#  * an optional list of default values (array only)
#  * an optional prompt phrase used when asking for the variable
#  * a list of values (enums only)
#vars:
#  project_name_dirt:
#    type: string
#    default: new_project
#    prompt: Project name?
#  project_org:
#    type: string
#    default: com.example
#    prompt: Type organization...
#  flavorizr:
#    type: boolean
#    default: true
#    prompt: Flavorize (DEV & PROD flavors will be added automatically)?
#  navigation:
#    type: enum
#    prompt: Which navigation would you like to generate?
#    values:
#      - goRouter
#      - autoRouter
#  localization:
#    type: enum
#    prompt: Localization made by flutter_gen or with Intl?
#    values:
#      - flutter_gen
#      - intl
#  device_preview:
#    type: boolean
#    default: false
#    prompt: Integrate Device Preview?
#  use_keytool:
#    type: boolean
#    default: true
#    prompt: Generate signing key?
#  use_sonar:
#    type: boolean
#    default: true
#    prompt: Will you use sonar?
#  theme_generate:
#    type: boolean
#    default: false
#    prompt: Create themes with theme_tailor?

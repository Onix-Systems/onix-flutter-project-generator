import 'package:${projectName}/data/model/remote/${className.snakeCase}/${className.snakeCase}_request.dart';
import 'package:${projectName}/domain/entity/${className.snakeCase}/${className.snakeCase}.dart';

class ${className.pascalCase}EntityToRequestMapper{
  
  ${className.pascalCase}Request map(${className.pascalCase} from) {
    return ${className.pascalCase}Request(
        name: from.name,
    );
  }
  
}
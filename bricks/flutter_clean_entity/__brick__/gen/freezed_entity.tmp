import 'package:freezed_annotation/freezed_annotation.dart';

part '${className.snakeCase}.freezed.dart';

@freezed
class ${className.pascalCase} with _$${className.pascalCase} {
    factory ${className.pascalCase}({
        required String name,
    }) = _${className.pascalCase};
}

${classBody}
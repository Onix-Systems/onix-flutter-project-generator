import 'package:{{projectName}}/core/arch/domain/common/converter/mapper.dart';
import 'package:${projectName}/data/model/remote/${sourceName.snakeCase}/${className.snakeCase}/${className.snakeCase}_request.dart';
import 'package:${projectName}/domain/entity/${sourceName.snakeCase}/${className.snakeCase}/${className.snakeCase}.dart';

class ${className.pascalCase}Mappers {
  final _${className.snakeCase}EntityToRequestMapper = _${className.pascalCase}EntityToRequestMapper();

  ${className.pascalCase}Request map${className.pascalCase}EntityToRequest(${className.pascalCase} entity) =>
      _${className.snakeCase}EntityToRequestMapper.map(entity);
}

class _${className.pascalCase}EntityToRequestMapper implements Mapper<${className.pascalCase}, ${className.pascalCase}Request>{
  @override
  ${className.pascalCase}Request map(${className.pascalCase} from) {
    return ${className.pascalCase}Request(
        name: from.name,
    );
  }
}
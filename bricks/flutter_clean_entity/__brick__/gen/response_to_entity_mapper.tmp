import 'package:${projectName}/data/model/remote/${className.snakeCase}/${className.snakeCase}_response.dart';
import 'package:${projectName}/domain/entity/${className.snakeCase}/${className.snakeCase}.dart';

class ${className.pascalCase}ResponseToEntityMapper{
  
  ${className.pascalCase}Entity map(${className.pascalCase}Response from) {
    return ${className.pascalCase}(
        name: from.name ?? '',
    );
  }
  
}
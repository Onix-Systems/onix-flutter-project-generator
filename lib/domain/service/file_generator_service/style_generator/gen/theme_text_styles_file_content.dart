import 'package:onix_flutter_bricks/domain/entity/app_styles/app_color_style.dart';
import 'package:onix_flutter_bricks/domain/entity/app_styles/app_text_style.dart';

class ThemeTextStylesFileContent {
//   static String generate(
//       {required List<AppTextStyle> textStyles, required bool useScreenUtil}) {
//     textStyles.sort((a, b) => a.name.compareTo(b.name));
//     return '''
//     import 'package:flutter/material.dart';
//
// abstract class AppColors {
//   //Light theme colors
//   ${sortedTextStyles['light']?.map((e) => 'static const Color ${e.name} = Color(0x${e.color.value.toRadixString(16)});').join('\n  ')}
//   //Dark theme colors
//   ${sortedTextStyles['dark']?.map((e) => 'static const Color ${e.name} = Color(0x${e.color.value.toRadixString(16)});').join('\n  ')}
//   //Common colors
//   ${sortedTextStyles['common']?.map((e) => 'static const Color ${e.name} = Color(0x${e.color.value.toRadixString(16)});').join('\n  ')}
// }''';
//   }
}
